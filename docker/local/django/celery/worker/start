#!/bin/bash

set -o errexit
set -o nounset

# `celery worker -A app` was removed in Celery 5; the app flag now comes first.
exec watchmedo auto-restart --directory=./ --pattern='*.py' --recursive -- \
  celery -A real_estate worker --loglevel=info
