# Runtime dependencies — pinned to the latest releases compatible with Django 6.1.1
# Django 6.1 requires Python >= 3.12.

# --- Core ---
Django==6.1.1
djangorestframework==3.18.1
djangorestframework-simplejwt==5.5.1

# --- Auth ---
# djoser pins social-auth-app-django to <6.0.0, so 5.9.0 is the newest usable release.
djoser==2.3.4
social-auth-app-django==5.9.0

# --- Django add-ons ---
django-cors-headers==4.9.0
django-countries==9.1.0
django-environ==0.14.0
django-filter==26.1
django-phonenumber-field[phonenumbers]==8.5.0
drf-spectacular==0.30.0

# --- Database / media ---
psycopg[binary]==3.3.5
Pillow==12.3.0

# --- Async tasks ---
celery==5.6.3
flower==2.1.0
redis==8.1.0

# --- WSGI server ---
gunicorn==26.2.0
